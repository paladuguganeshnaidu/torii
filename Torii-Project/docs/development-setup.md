# Development Setup (skeleton)

- Install Python 3.10+
- Create venv; pip install -r Backend/requirements.txt
- Run `python Backend/app.py`
- Serve frontend with Live Server or via Flask templates
